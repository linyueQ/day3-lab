# Spec 文档填充示例

本文档提供各模板文档的填充示例，供生成时参考。

## 08-系统架构与技术选型 填充示例

### 待填区域识别

```markdown
| 决策 | 选型 | 理由 |
|------|------|------|
| **前后端分离** | React SPA + Flask REST API | 教学清晰度：前端独立开发服务器 |
| **单体部署** | 前后端各一个进程 | 不引入微服务复杂度 |
| **分层架构** | Route → Agent → Storage | 替换存储引擎时上层零改动 |
| 【请填写第 4 条决策】 | 【选型】 | 【理由】 |
```

### 填充依据

从 03 提案 §5 风险中提取：
- 研报解析耗时较长 → 需要异步处理
- 用户等待体验 → 需要进度反馈

### 填充结果

```markdown
| **异步解析 + 状态轮询** | 研报解析后台任务 + GET parse-status 轮询接口 | 研报解析耗时较长（可能超 30s），轮询方式避免请求超时，前端可展示进度条并在完成后触发消息提醒 |
```

---

## 10-数据模型与存储规格 填充示例

### 待填区域识别

```markdown
| 方法 | 签名 | 行为 | 关联 TC |
|------|------|------|---------|
| `create_session` | `(session_id, title) → dict` | 创建新会话 | TC-M01-041 |
| `get_sessions` | `() → list` | 返回全部会话 | TC-M01-042 |
| `delete_session` | `(session_id) → None` | 删除 + **级联删除**记录 | TC-M01-043 |
| 【请补充：update_session】 | 【签名】 | 【行为】 | 【TC】 |
```

### 填充依据

从 09 API 规格 §8 DELETE /sessions/<id> 中提取：
- 级联删除关联记录
- 删除后不可恢复

从 05 US-002 AC 中提取：
- 会话标题首次问答后自动命名

### 填充结果

```markdown
| `update_session` | `(session_id, title, query_count) → None` | 更新会话标题与问答计数 | TC-M01-044 |
```

---

## 06-功能规格说明 填充示例

### 待填区域识别

```markdown
| 状态 | 条件 | 显示内容 |
|------|------|----------|
| **A 空状态** | `currentSession === null` | "请创建或选择一个会话开始" |
| **B 常见问题** | 有会话 && 无记录 | 【请填写：显示什么？点击后怎么做？】 |
| **C 对话历史** | 有记录 | 【请填写：每条记录卡片包含哪些元素？】 |
```

### 填充依据

从 05 US-003 验收标准：
- AC-002-01: 用户可在研报详情页面向AI提问
- AC-002-02: AI回答基于当前研报内容生成，并标注引用来源段落

从 09 API §11 POST /ask 响应：
- answer, referenced_segments, answer_source

### 填充结果

```markdown
| **B 常见问题** | 有会话 && 无记录 | 显示 3-6 个预设问题网格（如"这份研报的核心观点是什么？"）；点击后自动填充到输入框并发送 |
| **C 对话历史** | 有记录 | 每条记录卡片：问题气泡（右对齐）+ 回答气泡（左对齐）+ 来源标签 + 引用段落标注 + 时间戳 |
```

---

## 07-非功能需求与约束 填充示例

### 待填区域识别

```markdown
| 约束 | 规格 | 来源 |
|------|------|------|
| `query` 最小长度 | 1 字符 | `04` R-02 |
| `query` 最大长度 | 500 字符 | `04` R-02 |
| `session_id` 必填 | 非空字符串 | `04` R-01 |
| 【请补充：会话标题默认值】 | | |
```

### 填充依据

从 10 数据模型 §8 业务逻辑：
- 首次问答自动命名：标题 = query 前 20 字 + `...`

### 填充结果

```markdown
| 会话标题默认值 | "新会话"（≤ 23 字符），首次问答后自动重命名为 query 前 20 字 | `10` §8 业务逻辑 |
```

---

## 12-实施计划与里程碑 填充示例

### 待填区域识别

```markdown
| 阶段 | 交付要点（可演示） | 满足的 REQ/US | 验证 TC |
|------|-------------------|---------------|---------|
| **S1** 会话管理 | 会话 CRUD API + 前端侧栏 | REQ-001 部分 | TC-M01-020～025 |
| **S2** 问答核心 | 【请填写：核心问答交付要点】 | 【REQ】 | 【TC】 |
```

### 填充依据

从 05 US-003：
- 研报 AI 会话问答提交

从 09 API §11：
- POST /ask 端点

从 04 §5 能力优先级：
- 研报 AI 问答（含引用段落标注）= P0

### 填充结果

```markdown
| **S2** AI 问答核心 | 问答提交 API（三级降级）+ 新建会话 API + 前端研报详情页 AI 问答区 | US-003 / REQ-M1QA-003 | TC-RA-031～036 |
```

---

## 13-测试策略与质量门禁 填充示例

### 待填区域识别

```markdown
| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-020 | Integration | 【填写：如 GET /sessions → 200, 返回 sessions 数组】 | REQ-006 | P0 |
```

### 填充依据

从 09 API §7 GET /sessions 响应：
- traceId, total, sessions[]

从 05 US-002 AC-002-01：
- 可以查看到会话历史列表

### 填充结果

```markdown
| TC-RA-021 | L2 Integration | `GET /sessions` → 200，含 `sessions[]`、`total`、`traceId` | REQ-M1QA-002 | P0 |
```

---

## 14-需求追踪矩阵 填充示例

### 待填区域识别

```markdown
| 需求编号 | 需求名称 | 优先级 | 来源 | 详细说明 |
|----------|----------|--------|------|----------|
| **需求-M1-006** | 【填写：会话管理 CRUD】 | P0 | 【来源】 | 【详细说明】 |
```

### 填充依据

从 05 US-002：
- 故事：作为投研经理和高级分析师，我希望能够保留和查看跟AI的对话过程，以便追溯之前获取的信息

从 04 PRD §3 SC-03：
- 场景：查看与管理 AI 会话历史

### 填充结果

```markdown
| **REQ-M1QA-002** | AI 会话历史管理 | P0 | `05` US-002 | 用户可查看/追问/删除历史会话；删除需二次确认，支持全量清空 |
```

---

## 填充检查清单

每份文档生成后，检查以下内容：

- [ ] 模块编号、模块名称与 04 PRD 一致
- [ ] 所有【填写】【请填写】【请补充】区域已填充
- [ ] 章节引用格式正确（← → 对齐）
- [ ] 编号命名符合规范（REQ-、US-、AC-、TC-）
- [ ] 版本记录已添加
- [ ] 未删除模板中的引导语或示例
