# 请严格按照以下规则生成 Python 代码

1. 使用 Python 3.8+ 语法，遵循 PEP8 规范，变量/函数命名语义化、简洁易懂
2. 代码结构清晰，添加必要注释，不冗余、不废话
3. 必须包含：异常处理、类型注解（Type Hints）、边界判断
4. 优先使用标准库，如需第三方库，明确列出依赖和安装命令
5. 函数/类独立可复用，附带简单使用示例
6. 输出完整可运行代码，不要只给片段
7. 不输出多余解释，只给：代码 + 依赖（如有）+ 极简使用说明

需求：

1. 修改代码 E:\codes\fund_analysis\src\fetch_data\fetch_fund_info.py，先从接口 fund_name_em 获取全量基金信息，保存 基金代码、基金简称、基金类型 三个字段。根据接口 fund_individual_basic_info_xq 逐个更新基金其他字段，保留原来查询的逻辑。
