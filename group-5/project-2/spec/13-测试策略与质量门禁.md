# 命理测算工具 - 测试策略与质量门禁

> 文档编号: 13  
> 版本: V1.0  
> 日期: 2026-04-15  
> 作者: AI Spec Generator  
> 状态: 待审核

---

## 目录

- [1. 测试策略概述](#1-测试策略概述)
- [2. 测试类型](#2-测试类型)
- [3. 测试用例](#3-测试用例)
- [4. 测试环境](#4-测试环境)
- [5. 质量门禁](#5-质量门禁)
- [6. 测试自动化](#6-测试自动化)

---

## 1. 测试策略概述

### 1.1 测试原则

- **测试驱动**: 关键功能采用TDD(测试驱动开发)
- **自动化优先**: 尽可能自动化测试,减少人工测试
- **持续集成**: 每次提交代码自动运行测试
- **分层测试**: 单元测试、集成测试、E2E测试结合

### 1.2 测试覆盖目标

| 测试类型 | 覆盖率目标 | 工具 |
|---------|-----------|------|
| 单元测试 | ≥80% | Jest |
| 集成测试 | 核心API 100% | Jest + Supertest |
| E2E测试 | 关键路径100% | Playwright |
| 性能测试 | 核心接口100% | k6 |

### 1.3 测试阶段

```
开发阶段 → 单元测试 + 集成测试
    ↓
合并阶段 → 代码审查 + 自动化测试
    ↓
测试阶段 → 功能测试 + 性能测试 + 安全测试
    ↓
上线阶段 → 冒烟测试 + 监控告警
```

---

## 2. 测试类型

### 2.1 单元测试

**测试范围**:
- 工具函数
- 命理算法核心逻辑
- 数据验证逻辑
- 业务逻辑函数

**示例**:
```typescript
describe('八字排盘算法', () => {
  test('公历转农历正确', () => {
    const lunar = solarToLunar(1990, 5, 15)
    expect(lunar).toEqual({
      year: 1990,
      month: 4,
      day: 21,
      yearGanZhi: '庚午'
    })
  })
  
  test('四柱计算正确', () => {
    const pillars = calculatePillars({
      year: 1990,
      month: 4,
      day: 21,
      hour: 8
    })
    
    expect(pillars.yearPillar).toBe('庚午')
    expect(pillars.monthPillar).toBe('辛巳')
    expect(pillars.dayPillar).toBe('壬辰')
    expect(pillars.hourPillar).toBe('甲辰')
  })
})
```

### 2.2 集成测试

**测试范围**:
- API接口
- 数据库操作
- 缓存机制
- 认证流程

**示例**:
```typescript
describe('用户认证API', () => {
  test('用户注册成功', async () => {
    const response = await request(app)
      .post('/api/auth/register')
      .send({
        email: 'test@example.com',
        password: 'SecurePass123',
        nickname: '测试用户'
      })
    
    expect(response.status).toBe(201)
    expect(response.body.success).toBe(true)
    expect(response.body.data.token).toBeDefined()
  })
  
  test('重复注册失败', async () => {
    const response = await request(app)
      .post('/api/auth/register')
      .send({
        email: 'test@example.com',
        password: 'SecurePass123',
        nickname: '测试用户'
      })
    
    expect(response.status).toBe(409)
    expect(response.body.error.code).toBe('EMAIL_EXISTS')
  })
})
```

### 2.3 E2E测试

**测试范围**:
- 关键用户流程
- 跨页面交互
- 响应式适配
- 浏览器兼容性

**示例**:
```typescript
test('用户完整使用流程', async ({ page }) => {
  // 1. 注册
  await page.goto('http://localhost:3000/register')
  await page.fill('[name="email"]', 'test@example.com')
  await page.fill('[name="password"]', 'SecurePass123')
  await page.click('button[type="submit"]')
  
  // 2. 填写档案
  await page.waitForURL('**/profile')
  await page.fill('[name="nickname"]', '测试用户')
  await page.fill('[name="birthDate"]', '1990-05-15')
  await page.click('button[type="submit"]')
  
  // 3. 查看黄历
  await page.waitForURL('**/')
  await expect(page.locator('.calendar-card')).toBeVisible()
  
  // 4. 运势问询
  await page.click('text=运势问询')
  await page.fill('[name="question"]', '今天适合签约吗?')
  await page.click('button[type="submit"]')
  await expect(page.locator('.result')).toBeVisible()
})
```

### 2.4 性能测试

**测试场景**:
```javascript
// k6性能测试脚本
import http from 'k6/http'
import { check, sleep } from 'k6'

export const options = {
  vus: 100, // 100并发用户
  duration: '5m', // 持续5分钟
  thresholds: {
    http_req_duration: ['p(95)<500'], // 95%请求<500ms
    http_req_failed: ['rate<0.01'], // 错误率<1%
  },
}

export default function () {
  const res = http.get('https://api.xxx.com/api/calendar/today', {
    headers: { 'Authorization': `Bearer ${token}` }
  })
  
  check(res, {
    'status is 200': (r) => r.status === 200,
    'response time < 500ms': (r) => r.timings.duration < 500,
  })
  
  sleep(1)
}
```

### 2.5 安全测试

**测试项**:
- SQL注入
- XSS攻击
- CSRF攻击
- 认证绕过
- 权限提升
- 敏感数据泄露

**工具**:
- OWASP ZAP
- npm audit
- Snyk
- SonarQube

---

## 3. 测试用例

### 3.1 用户系统测试

**TC-AUTH-001: 用户注册成功**

**前置条件**: 无

**测试步骤**:
1. 访问注册页面
2. 输入有效邮箱、密码、昵称
3. 点击注册按钮

**预期结果**: 
- 注册成功,跳转到档案页面
- 数据库创建用户记录
- 密码加密存储

**优先级**: P0

---

**TC-AUTH-002: 密码强度校验**

**前置条件**: 无

**测试步骤**:
1. 访问注册页面
2. 输入弱密码(如"123456")
3. 点击注册按钮

**预期结果**: 
- 显示密码强度不足提示
- 注册失败

**优先级**: P0

---

**TC-AUTH-003: 登录成功**

**前置条件**: 用户已注册

**测试步骤**:
1. 访问登录页面
2. 输入正确的邮箱和密码
3. 点击登录按钮

**预期结果**: 
- 登录成功,跳转到首页
- 签发JWT Token
- Cookie设置HttpOnly

**优先级**: P0

---

**TC-AUTH-004: Token过期处理**

**前置条件**: 用户Token已过期

**测试步骤**:
1. 使用过期Token访问需要认证的页面
2. 观察系统反应

**预期结果**: 
- 自动跳转到登录页
- 显示"登录已过期"提示

**优先级**: P0

---

### 3.2 黄历功能测试

**TC-CALENDAR-001: 今日黄历展示**

**前置条件**: 用户已登录且有档案

**测试步骤**:
1. 访问首页
2. 查看黄历卡片

**预期结果**: 
- 显示今日黄历
- 包含适宜/不适宜事项
- 包含幸运颜色和方向
- 内容基于用户八字个性化

**优先级**: P0

---

**TC-CALENDAR-002: 日期切换**

**前置条件**: 用户在首页

**测试步骤**:
1. 点击右箭头(下一天)
2. 点击左箭头(前一天)
3. 尝试查看超过7天的日期

**预期结果**: 
- 成功切换到指定日期
- 超出范围时按钮禁用
- 切换流畅无卡顿

**优先级**: P1

---

### 3.3 运势问询测试

**TC-DIVINATION-001: 奇门遁甲起卦**

**前置条件**: 用户已登录,今日未使用过奇门

**测试步骤**:
1. 进入运势问询页面
2. 选择奇门遁甲
3. 输入测算事项
4. 授权位置信息
5. 点击起卦按钮

**预期结果**: 
- 显示卦象结果
- 包含建议和成功率
- 保存到历史记录
- 今日剩余次数显示为0

**优先级**: P0

---

**TC-DIVINATION-002: 奇门遁甲次数限制**

**前置条件**: 今日已使用1次奇门遁甲

**测试步骤**:
1. 尝试再次起卦

**预期结果**: 
- 提示"今日仅限1次"
- 起卦按钮禁用

**优先级**: P0

---

**TC-DIVINATION-003: 大小六壬起卦**

**前置条件**: 用户已登录

**测试步骤**:
1. 选择大小六壬
2. 输入测算事项
3. 输入3个数字(1-12)
4. 点击起卦按钮

**预期结果**: 
- 显示卦象结果
- 不同数字产生不同结果
- 今日剩余次数-1

**优先级**: P0

---

### 3.4 命格解析测试

**TC-DESTINY-001: 本人命格解析**

**前置条件**: 用户已登录且有档案

**测试步骤**:
1. 进入命格解析页面
2. 选择"解析本人"
3. 点击开始解析

**预期结果**: 
- 显示命格关键词
- 显示五行分析
- 显示性格、事业、感情、健康分析
- 总字数800-1500字

**优先级**: P0

---

**TC-DESTINY-002: 他人命格解析**

**前置条件**: 用户已登录

**测试步骤**:
1. 选择"解析他人"
2. 输入他人信息
3. 点击开始解析

**预期结果**: 
- 成功生成他人命格解析
- 保存到历史记录

**优先级**: P0

---

**TC-DESTINY-003: 无时辰解析**

**前置条件**: 用户已登录

**测试步骤**:
1. 输入出生日期
2. 勾选"不清楚具体时辰"
3. 点击开始解析

**预期结果**: 
- 成功生成解析(仅三柱)
- 页面标注"精度降低"

**优先级**: P0

---

## 4. 测试环境

### 4.1 环境配置

| 环境 | 用途 | 配置 |
|------|------|------|
| Local | 本地开发测试 | 开发者本地环境 |
| Staging | 集成测试 | 与生产环境相同的配置 |
| Production | 冒烟测试 | 生产环境 |

### 4.2 测试数据

**测试账号**:
```
测试用户1:
  email: test1@example.com
  password: TestPass123
  bazi: 庚午年 辛巳月 壬辰日 甲辰时

测试用户2:
  email: test2@example.com
  password: TestPass123
  bazi: 甲子年 丙寅月 戊午日 庚申时
```

**测试数据准备**:
```bash
# 执行测试数据种子脚本
npm run seed:test
```

### 4.3 浏览器测试矩阵

| 浏览器 | 版本 | 平台 | 优先级 |
|--------|------|------|--------|
| Chrome | 最新版 | Desktop/Mobile | P0 |
| Safari | 最新版 | Desktop/iOS | P0 |
| Firefox | 最新版 | Desktop | P1 |
| Edge | 最新版 | Desktop | P1 |

---

## 5. 质量门禁

### 5.1 代码提交门禁

**Pre-commit Hook**:
```bash
#!/bin/bash

# 运行lint
npm run lint

# 运行单元测试
npm run test:unit

# 检查测试覆盖率
npm run test:coverage

# 如果任何一步失败,阻止提交
if [ $? -ne 0 ]; then
  echo "代码检查失败,阻止提交"
  exit 1
fi
```

### 5.2 CI门禁

**GitHub Actions检查项**:
```yaml
name: Quality Gate
on: [pull_request]

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Lint
        run: npm run lint
        
      - name: Unit Tests
        run: npm run test:unit
        
      - name: Integration Tests
        run: npm run test:integration
        
      - name: Coverage
        run: npm run test:coverage
        
      - name: Check Coverage Threshold
        run: |
          COVERAGE=$(npm run test:coverage -- --json | jq '.total.lines.pct')
          if (( $(echo "$COVERAGE < 80" | bc -l) )); then
            echo "测试覆盖率不足80%"
            exit 1
          fi
```

### 5.3 合并门禁

**Pull Request要求**:
- [ ] 所有CI检查通过
- [ ] 代码审查通过(至少1人)
- [ ] 测试覆盖率≥80%
- [ ] 无P0/P1级别Bug
- [ ] 文档已更新

### 5.4 发布门禁

**上线前检查**:
- [ ] 功能测试100%通过
- [ ] 性能测试达标
- [ ] 安全测试通过
- [ ] E2E测试通过
- [ ] 代码审查完成
- [ ] 文档完整
- [ ] 回滚方案就绪

---

## 6. 测试自动化

### 6.1 CI/CD流程

```yaml
name: CI/CD Pipeline
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'
      - name: Install dependencies
        run: npm ci
      - name: Run lint
        run: npm run lint
      - name: Run unit tests
        run: npm run test:unit
      - name: Run integration tests
        run: npm run test:integration
      - name: Upload coverage
        uses: codecov/codecov-action@v2
        
  e2e:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v2
      - name: Run E2E tests
        run: npm run test:e2e
        
  deploy:
    runs-on: ubuntu-latest
    needs: e2e
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy to production
        run: ./deploy.sh
```

### 6.2 测试报告

**覆盖率报告**:
```
运行: npm run test:coverage

输出:
-------------------|---------|----------|---------|---------|
File               | % Stmts | % Branch | % Funcs | % Lines |
-------------------|---------|----------|---------|---------|
All files          |   85.2  |   78.5   |   82.1  |   86.3  |
 algorithms/       |   92.1  |   85.3   |   90.5  |   93.2  |
 controllers/      |   78.5  |   72.1   |   75.0  |   79.8  |
 services/         |   88.3  |   80.2   |   85.7  |   89.1  |
-------------------|---------|----------|---------|---------|
```

**测试执行报告**:
```
Test Suites: 15 passed, 15 total
Tests:       128 passed, 128 total
Snapshots:   0 total
Time:        12.5s
```

---

## 文档审批

| 角色 | 姓名 | 签字 | 日期 |
|------|------|------|------|
| 测试负责人 | | | |
| 技术负责人 | | | |

---

**文档结束**
